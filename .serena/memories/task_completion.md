When finishing code changes in this project, run the full unit test suite with python -m unittest discover -s tests -p "test_*.py". For release-sensitive changes affecting startup or solver behavior, also run powershell -ExecutionPolicy Bypass -File .\release_smoke.ps1 when the environment supports Camoufox/Patchright. If new environment variables are introduced, update both .env.example and README.md in the same change. Do not commit local artifacts from logs/, keys/, or .env.